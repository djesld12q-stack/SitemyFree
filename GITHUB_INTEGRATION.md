# GitHub Інтеграція — Управління Контентом

Це система для управління скриптами, посиланнями та іграми через GitHub API без необхідності в бекенді.

## 🎮 Управління іграми

Коли ви додаєте/редагуєте гру як `sun69wukong`:

1. Гра **автоматично зберігається в `games.json`** на GitHub
2. Сторінка ігор **завантажує дані з GitHub** і показує їх **ВСІМ ВІДВІДУВАЧАМ**
3. Ви отримуєте повідомлення "Гру додано! Вона видна всім на сайті!"

### HTML структура гри в games.json:

```json
{
  "done": [
    {
      "id": 100,
      "title": "Darwin's Paradox!",
      "img": "https://...",
      "rating": "8.5",
      "review": "Гра цікава але важка..."
    }
  ]
}
```

### Що видно на сайті:

HTML генерується **динамічно** з JSON:

```html
<div class="game-card" data-title="Darwin's Paradox!" data-img="https://..." data-rating="8.5" data-review="Гра цікава...">
  <img src="https://...">
  <div class="card-hover-hint">★ ПЕРЕГЛЯНУТИ ВІДГУК</div>
  <div class="game-card-overlay">✓ ПРОЙДЕНО</div>
  <div class="card-rating-chip">★ 8.5/10</div>
  <div class="game-card-body">
    <div class="game-card-title">Darwin's Paradox!</div>
    <div class="game-card-meta">
      <span class="game-badge done">✓ ПРОЙДЕНО</span>
      <span class="game-platform">🎮</span>
    </div>
  </div>
</div>
```

## 🔧 Налаштування

### 1. GitHub Repository Setup

Переконайтеся, що в вашому репозиторії є ці JSON файли:
- `scripts.json` — список скриптів
- `links.json` — список посилань  
- `games.json` — список ігор за категоріями:
  - `playing` — граю зараз
  - `done` — пройдені
  - `dropped` — закинуті
  - `planned` — заплановані

### 2. Personal Access Token

1. Перейдіть на https://github.com/settings/tokens
2. Натисніть **"Generate new token"** → **"Generate new token (classic)"**
3. Додайте опис (наприклад: "sunwukong-admin")
4. Виберіть scopes:
   - ✅ `repo` (повний доступ до репозиторія)
   - ✅ `read:user` (читання профілю)
5. Натисніть **"Generate token"**
6. **Скопіюйте токен** (він більше не буде видимий!)

### 3. Вставлення токена

Коли ви авторизуєтеся через Twitch як **sun69wukong**, система попросить вас ввести токен:

```
Введіть GitHub Personal Access Token для управління іграми:
(Токен буде збережений локально)
```

Вставте ваш токен, і він буде збережений в `localStorage` браузера.

## 📝 Управління контентом

### Додавання гри

1. Авторизуйтеся через Twitch як **sun69wukong**
2. На сторінці ігор натисніть **"+ ДОДАТИ"**
3. Заповніть форму:
   - **НАЗВА ГРИ** — назва гри
   - **URL КАРТИНКИ** — посилання на обкладинку
   - **КАТЕГОРІЯ** — вибір категорії (граю, пройдено, закинуто, заплановане)
   - **ОЦІНКА** — від 1 до 10 (опціонально)
   - **ВІДГУК** — ваш відгук про гру (опціонально)
4. Натисніть **"✓ ЗБЕРЕГТИ"**

**Що відбудеться:**
- Гра додається в `games.json` на GitHub
- GitHub відправляє commit
- ВСІ користувачи бачать нову гру на сайті! 🎉

### Редагування гри

1. Наведіть мишку на карточку гри
2. Натисніть кнопку **"✏ EDIT"** (видна тільки для вас)
3. Змініть потрібні поля
4. Натисніть **"✓ ЗБЕРЕГТИ"**

### Видалення гри

1. Відкрийте гру для редагування
2. Натисніть **"✗ ВИДАЛИТИ"**
3. Підтвердіть видалення

## 🔒 Безпека

⚠️ **ВАЖЛИВО:**

- **Ніколи не діліться токеном** публічно
- Токен зберігається тільки в `localStorage` браузера (не відправляється на сервер)
- Якщо ви випадково поділилися токеном:
  1. Негайно зайдіть на https://github.com/settings/tokens
  2. Видаліть скомпрометований токен
  3. Створіть новий

## 📋 Структура JSON файлів

### games.json
```json
{
  "playing": [
    {
      "id": 1,
      "title": "Назва гри",
      "img": "https://...",
      "rating": "",
      "review": ""
    }
  ],
  "done": [
    {
      "id": 100,
      "title": "Пройдена гра",
      "img": "https://...",
      "rating": "8.5",
      "review": "Мій відгук..."
    }
  ],
  "dropped": [],
  "planned": []
}
```

### scripts.json
```json
[
  {
    "id": 1,
    "name": "Назва скрипту",
    "desc": "Опис",
    "img": "https://...",
    "url": "https://...",
    "createdAt": "2026-04-24T10:00:00Z"
  }
]
```

### links.json
```json
[
  {
    "id": 1,
    "name": "Назва посилання",
    "handle": "Опис або username",
    "url": "https://...",
    "img": "https://...",
    "donate": false
  }
]
```

## 🐛 Вирішення проблем

### "GitHub токен не встановлений"
- Переконайтеся, що ви авторизовані як **sun69wukong** через Twitch
- Вставте токен, коли система запросить

### "Помилка при збереженні"
- Перевірте, що токен має права на запис (`repo` scope)
- Перевірте інтернет-з'єднання
- Переспробуйте операцію

### "Файл не знайдений на GitHub"
- Переконайтеся, що `scripts.json`, `links.json` і `games.json` існують в репозиторії
- Якщо файли відсутні, вони будуть автоматично створені при першому збереженні

## 📚 Як це працює

```
🔐 Авторизація Twitch
    ↓
🔑 Запит GitHub токена
    ↓
📝 Редагування контенту (+ ДОДАТИ, ✏ EDIT)
    ↓
📤 Запис в GitHub через API (games.json)
    ↓
🌐 Публічне завантаження для всіх користувачів
    ↓
💻 Генерація HTML карточок динамічно
```

Немає потреби в бекенді — все працює напрямку з GitHub API! 🚀

### Послідовність подій при додаванні гри:

1. Ви натискаєте "+ ДОДАТИ"
2. Заповнюєте форму (назва, картинка, оцінка, відгук)
3. Натискаєте "ЗБЕРЕГТИ"
4. Система відправляє запит до GitHub API
5. GitHub оновлює `games.json` файл
6. GitHub робить автоматичний commit
7. Ваша гра з'являється в `games.json`
8. **ВСІМА видна на сайті!**
9. Ви отримуєте повідомлення про успіх

Так легко! 🎮✨
